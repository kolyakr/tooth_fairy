# ML inference stack (optional — install alongside requirements.txt for live YOLO runs)
# conda activate ml-env  # recommended per AGENTS.md
# Linux x86_64: PyPI resolves torch with NVIDIA CUDA wheels (large). On small builders use
# ``pip install --no-cache-dir`` to reduce peak disk (see docs/DEPLOYMENT.md).
torch>=2.4.0,<3
ultralytics>=8.3.0,<9
opencv-python-headless>=4.10.0,<5
numpy>=1.26.0,<3
