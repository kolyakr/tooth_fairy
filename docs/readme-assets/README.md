# README assets

Files referenced from the root [`README.md`](../README.md):

| File | Purpose |
|------|---------|
| `pipeline-overview.png` (or `.svg`) | Optional high-res pipeline figure for slides; the main architecture diagram lives as **Mermaid** in the root [`README.md`](../README.md) **Architecture** section. |

The **Demo** screencast in the root README is [`../uploads/tooth_fairy_test.mp4`](../uploads/tooth_fairy_test.mp4).

Optional extra figures for the readme (the **Model output examples** section now uses committed files under [`../../modeling/outputs/`](../../modeling/outputs/) from `run_inference.py`).

Use `.jpg` instead of `.png` if preferred—then update the image paths in the root `README.md`.
